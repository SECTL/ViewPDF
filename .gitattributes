# 换行符统一：所有文本文件入库一律 LF，检出时也保持 LF
# （避免 CRLF/LF 混用造成脚本改写文件后的整文件行尾噪音 diff）
* text=auto eol=lf

# 二进制资源显式声明，防止 git 自动检测误判为文本而损坏
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.ttf binary
*.pfb binary
*.bcmap binary
*.woff binary
*.woff2 binary
